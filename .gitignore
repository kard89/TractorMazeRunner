# --- Visual Studio ---
.vs/
*.user
*.suo
*.opensdf
*.sdf
*.cache
*.ipch
*.aprof

# --- Pliki wynikowe kompilacji (Build Output) ---
# Te foldery tworzą się same po naciśnięciu "Build"
x64/
x86/
Debug/
Release/
build/
bin/
obj/

# --- Konkretne rozszerzenia plików tymczasowych ---
*.obj
*.pdb
*.ilk
*.tlog
*.idb
*.log
*.pch
*.res

# --- Pliki wykonywalne (chyba że chcesz je trzymać) ---
*.exe
*.lib

# --- DLL ---
# Zazwyczaj ignorujemy DLL-ki generowane przez build,
# ALE chcemy zachować tę jedną główną SDL2.dll w folderze głównym.
*.dll
!SDL2.dll
!SDL2_ttf.dll

# --- Systemowe ---
Thumbs.db
.DS_Store
# --- WYJĄTKI DLA SDL2 (Naprawa brakujących bibliotek) ---

# 1. Odblokuj folder lib wewnątrz SDL2
!dependencies/SDL2/lib/

# 2. Odblokuj podfoldery architektur (nadpisuje blokadę x64/ i x86/)
!dependencies/SDL2/lib/x64/
!dependencies/SDL2/lib/x86/

# 3. KLUCZOWE: Odblokuj pliki .lib w tych folderach (nadpisuje blokadę *.lib)
!dependencies/SDL2/lib/x64/*.lib
!dependencies/SDL2/lib/x86/*.lib
